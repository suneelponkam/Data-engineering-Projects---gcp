1.  Apache beam installation :
> installing the apache_beam with pip

!pip install apache_beam[interactive]

> importing the apache_beam

import apache_beam as beam

> beam pipeline 
p1 = beam.Pipeline()